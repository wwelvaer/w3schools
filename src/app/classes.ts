export interface Ref {
  name: string;
  url: string;
}

export interface Customer {
  id: number,
  name: string,
  contact: string,
  address: string,
  city: string,
  postalcode: string,
  country: string
}

